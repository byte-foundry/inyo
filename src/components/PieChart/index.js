import styled from '@emotion/styled';
import React from 'react';

import {
	accentGrey,
	primaryBlue,
	primaryRed,
	primaryWhite,
} from '../../utils/colors';
import {Pie} from '../../utils/new/design-system';

const PieChart = props => <Pie {...props} />;

export default PieChart;
